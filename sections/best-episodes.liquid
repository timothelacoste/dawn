<link href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" rel="stylesheet">
{{ 'best-episodes.css' | asset_url | stylesheet_tag }}

<section class="best-episodes">
  {{ section.settings.title }}
  <div class="swiper" id="swiper-{{ section.id }}">
    <div class="swiper-wrapper">
      {% for block in section.blocks %}
        <div class="swiper-slide">
          <a href="{{ block.settings.episode-url }}">
            <img
              src="{{ block.settings.image | image_url }}"
              alt="{{ block.settings.alt }}"
              height="{{ block.settings.image.height }}"
              width="{{ block.settings.image.width }}"
            >
          </a>
        </div>
      {% endfor %}
    </div>
    <div class="swiper-button-prev"><span class="visually-hidden">prev</span></div>
    <div class="swiper-button-next"><span class="visually-hidden">next</span></div>
  </div>
</section>

<script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
<script>
  const swiper = new Swiper('#swiper-{{ section.id }}', {
    direction: 'horizontal',
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
    breakpoints: {
      100: {
        slidesPerView: 1,
        spaceBetween: 20,
      },
      769: {
        slidesPerView: 3,
        spaceBetween: 50,
      },
    },
  });
</script>

{% schema %}
{
  "name": "Best Episodes",
  "settings": [
    {
      "type": "richtext",
      "id": "title",
      "label": "Title",
      "default": "<h2>Les épisodes coups de coeur</h2>"
    }
  ],
  "blocks": [
    {
      "type": "image",
      "name": "An Episode",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "url",
          "id": "episode-url",
          "label": "Episode Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Best Episodes"
    }
  ]
}
{% endschema %}
